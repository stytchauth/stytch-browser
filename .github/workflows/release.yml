name: Release

on:
  push:
    branches:
      - main
      - v5
  # Allows re-running just the Mintlify docs update without cutting a release
  workflow_dispatch:
    inputs:
      js_sdk_ref:
        description: 'Ref of this repo that the docs should be generated from'
        required: false
        default: 'main'

concurrency: ${{ github.workflow }}-${{ github.ref }}

jobs:
  release:
    name: Release
    if: github.event_name == 'push'
    runs-on: ubuntu-latest-public
    permissions:
      id-token: write # Required for OIDC
      contents: read
    outputs:
      published: ${{ steps.changesets.outputs.published }}
    steps:
      - name: Checkout Repo
        uses: actions/checkout@f43a0e5ff2bd294095638e18286ca9a3d1956744 # v3.6.0
        with:
          token: ${{ secrets.PERSONAL_ACCESS_TOKEN }}

      - name: Set up Git
        env:
          TOKEN: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
        run: |
          # Configure git
          git config --global url."https://ci-stytch:${TOKEN}@github.com/stytchauth".insteadOf "https://github.com/stytchauth"
          git config --global user.email "ci-stytch@stytch.com"
          git config --global user.name "ci-stytch"

      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version-file: .nvmrc
          registry-url: 'https://registry.npmjs.org'
          cache: 'yarn'

      # This step is needed because npm only supported OIDC publishing with v11.15.1 and setup-node
      # installs an older version
      - name: Update npm
        run: |
          npm install -g npm@latest
          npm --version

      - name: Install Dependencies
        run: yarn

      - name: Build
        run: yarn build

      - name: Create Release Pull Request or Publish
        id: changesets
        uses: changesets/action@6a0a831ff30acef54f2c6aa1cbbc1096b066edaf # v1.7.0
        with:
          setupGitUser: false
          publish: yarn changeset publish
        env:
          GITHUB_TOKEN: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
          NPM_TOKEN: ''
          NPM_CONFIG_PROVENANCE: false

  update_mintlify_docs:
    name: Update Mintlify docs
    needs: release
    if: |
      !cancelled() &&
      (github.event_name == 'workflow_dispatch' || needs.release.outputs.published == 'true')
    runs-on: ubuntu-latest-public
    steps:
      - name: Checkout Repo
        uses: actions/checkout@f43a0e5ff2bd294095638e18286ca9a3d1956744 # v3.6.0
        with:
          persist-credentials: false

      - name: Trigger Mintlify docs update
        run: |
          # Trigger the mintlify-docs workflow to update changelogs
          gh workflow run js-sdk-docs.yml \
            --repo stytchauth/mintlify-docs \
            --ref main \
            --field js_sdk_ref="${JS_SDK_REF}"
        env:
          GITHUB_TOKEN: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
          JS_SDK_REF: ${{ inputs.js_sdk_ref || 'main' }}
