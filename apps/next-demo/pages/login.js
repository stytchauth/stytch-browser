import { defaultDarkTheme, defaultTheme, Products, StytchLogin, useStytchSession } from '@stytch/nextjs';
import Head from 'next/head';
import { useRouter } from 'next/router';
import React, { useEffect } from 'react';

import styles from '../styles/Home.module.css';

const STYTCH_LOGO_IMG_URL = 'https://public-assets.stytch.com/stytch_logo_charcoal.png';

const stytchConfig = {
  products: [Products.otp],
  otpOptions: {
    methods: ['email'],
    expirationMinutes: 10,
  },
};

const presentation = {
  theme: [defaultTheme, defaultDarkTheme],
  logo: {
    url: STYTCH_LOGO_IMG_URL,
  },
};

export default function Login() {
  const router = useRouter();
  const { session, isInitialized } = useStytchSession();

  useEffect(() => {
    if (isInitialized && session !== null) {
      router.push('/profile');
    }
  }, [session, isInitialized, router]);

  return (
    <div className={styles.container}>
      <Head>
        <title>Stytch - Create Next App</title>
        <meta name="description" content="Generated by create next app" />
      </Head>

      <main className={styles.main}>
        <h1 className={styles.title}>
          Welcome to <a href="https://nextjs.org">Next.js..!</a>
        </h1>
        <StytchLogin config={stytchConfig} presentation={presentation} />
      </main>
    </div>
  );
}
